<Window x:Class="PROG6221.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cybersecurity Chat" Height="600" Width="600"
        Background="#2C003E" Foreground="White" WindowStartupLocation="CenterScreen">
	<Grid Margin="15">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="200"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" Text="Cybersecurity Awareness Bot" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

		<DataGrid x:Name="dgTasks" Grid.Row="1" AutoGenerateColumns="False" Margin="0,5,0,15" Background="White" Foreground="Black">
			<DataGrid.Columns>
				<DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="30" IsReadOnly="True"/>
				<DataGridTextColumn Header="Task Title" Binding="{Binding Title}" Width="*"/>
				<DataGridCheckBoxColumn Header="Done" Binding="{Binding IsComplete}" Width="50"/>
			</DataGrid.Columns>
		</DataGrid>

		<TextBox x:Name="txtChat" Grid.Row="2" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Padding="5" Background="#1E002B" Foreground="LightGreen" FontFamily="Consolas"/>

		<TextBox x:Name="txtInput" Grid.Row="3" Margin="0,15,0,0" Height="30" Padding="5" VerticalContentAlignment="Center" KeyDown="txtInput_KeyDown"/>

		<StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
			<Button x:Name="btnSEND" Content="Send" Width="90" Height="30" Margin="5" Click="btnSEND_Click" Background="#4A0072" Foreground="White"/>
			<Button x:Name="btnQuiz" Content="Start Quiz" Width="90" Height="30" Margin="5" Click="btnQuiz_Click" Background="#4A0072" Foreground="White"/>
			<Button x:Name="btnActivityLog" Content="Activity Log" Width="100" Height="30" Margin="5" Click="btnActivityLog_Click" Background="#4A0072" Foreground="White"/>
		</StackPanel>
	</Grid>
</Window>